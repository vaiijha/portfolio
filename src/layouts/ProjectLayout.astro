---
import BaseLayout from "./BaseLayout.astro";

const { title, description, badge, stack, dateRange } = Astro.props;
---

<BaseLayout title={title} description={description} sideBarActiveItemID="projects">
  <article class="prose prose-lg max-w-prose prose-headings:tracking-tight">
    <p class="not-prose mb-2">
      <a href="/projects" class="text-sm link link-hover no-underline">← All projects</a>
    </p>

    <h1 class="title my-2 text-3xl sm:text-4xl font-bold leading-tight">{title}</h1>

    {
      (badge || dateRange || stack) && (
        <div class="not-prose flex flex-wrap items-center gap-2 mb-4 text-sm text-base-content/70">
          {badge && <span class="badge badge-ghost">{badge}</span>}
          {dateRange && <span>{dateRange}</span>}
          {stack && <span class="text-base-content/55">· {stack}</span>}
        </div>
      )
    }

    <div class="divider my-4 not-prose" />
    <slot />
  </article>
</BaseLayout>
